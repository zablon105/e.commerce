Minimal Express server to hold products and feedback for the Praise demo.

To run:

```
cd server
npm install
npm start
```

API endpoints:
- GET /api/products
- POST /api/products
- PUT /api/products/:id
- DELETE /api/products/:id
- GET /api/feedback
- POST /api/feedback
